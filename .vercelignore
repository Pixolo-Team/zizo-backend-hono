# Exclude the TypeScript edge function source from Vercel's TypeScript compiler.
# Vercel deploys the pre-bundled api/index.js (generated by `npm run build`)
# instead of recompiling from the source .ts file.
api/index.ts
