/**
 * ---------------------------------------------------------
 * AUTH CONTROLLER
 * ---------------------------------------------------------
 * Purpose:
 * Handles HTTP request and response logic for authentication.
 *
 * Responsibilities:
 * - Read request body / headers
 * - Validate basic request data
 * - Call auth service functions
 * - Return proper HTTP responses
 *
 * Important:
 * - Should NOT contain business logic
 * - Should NOT directly communicate with database
 * - Only handles HTTP layer
 *
 * Flow:
 * Controller → Service
 * ---------------------------------------------------------
 */
